package stack;

import stack.ListStack.ListStack;

public class PostFixInterpreter {

    private ListStack<Double> stack;

    public PostFixInterpreter() {
	stack = new ListStack<Double>();
    }

    public double interpret(String s) {
	for (int i = 0; i < s.length(); i++) {
	    if (s.charAt(i) >= '0' && s.charAt(i) <= '9') {
		stack.push((double) s.charAt(i) - '0');
		//System.out.println(stack.top());
	    } else if (s.charAt(i) == '+') {
		stack.push(stack.poptop()+stack.poptop());
		//System.out.println(stack.top());

	    } else if (s.charAt(i) == '-') {
		stack.push(stack.poptop()-stack.poptop());
		//System.out.println(stack.top());

	    } else if (s.charAt(i) == '*') {
		stack.push(stack.poptop()*stack.poptop());

	    } else if (s.charAt(i) == '/') {
		stack.push(stack.poptop()/stack.poptop());

	    } else if (s.charAt(i) == '!') {
		System.out.println("This would blow the complexity.");
	    } else if (s.charAt(i) == '^') {
		stack.push(Math.pow(stack.poptop(),stack.poptop()));
		//System.out.println(stack.top());

	    }
	}
	//System.out.println(stack.top());

	return stack.poptop();
    }
    
    public static void main(String[] args) {
	PostFixInterpreter interpreter = new PostFixInterpreter();
	String calculation = "12+34^+";
	System.out.println(interpreter.interpret(calculation));
    }

}
